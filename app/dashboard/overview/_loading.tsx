import DashboardSkeleton from '@ui/skeletons';

function Loading() {
    return <DashboardSkeleton />;
}

export default Loading;

// For page level loading, add folder (overview) and loading inside with page.tsx as well
